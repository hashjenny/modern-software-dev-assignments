{
  "name": "weather-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for weather and clothing advice",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "cli": "node build/cli.js",
    "mcp": "node build/index.js",
    "dev": "tsc && node build/index.js",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^17.3.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.2"
  }
}
